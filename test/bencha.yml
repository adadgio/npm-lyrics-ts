config:
  target: "http://localhost:8182"
  phases:
    - duration: 50
      arrivalRate: 200
  defaults:
    headers:
      x-my-service-auth: "987401838271002188298567"

scenarios:
  - flow:
    - get:
        url: "/default/get-demo"
  - flow:
    - post:
        url: "/default/post-demo"
        json: { "id": 1234,  "name": "Benassi", "surname": "Benny" }
